# GH Workspace Manager V4.1 Source Update

Copy these files into the root of your existing `GH-Workspace-Manager` repository.

## Run

From the repository root:

```bash
python3 src/main.py
```

## Commit

```bash
git status
git add .
git commit -m "Add Version 4.1 dashboard and workspace health checks"
git push
```
